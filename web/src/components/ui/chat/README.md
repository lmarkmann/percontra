# Conversation primitives

Chat-specific components composed as **MessageScroller -> Message -> Bubble / Attachment / Marker**.

Generic form primitives (`input`, `input-group`, `button`) stay in `src/components/ui/`.

Import from `@/components/ui/chat`.
