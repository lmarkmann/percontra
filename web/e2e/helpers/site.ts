// Keep in sync with `name` in src/lib/site.ts (that module pulls @/env,
// which Playwright's transform cannot resolve). Renaming the product
// means changing exactly these two spots; grep: siteName.
export const siteName = "vite-template";
