# Nothing here is crawlable: the origin sits behind Cloudflare Access and
# public/_headers sends X-Robots-Tag: noindex,nofollow for every path. This file
# is inert, and is kept only so a crawler gets a 200 instead of the SPA shell.
#
# Keep this a SINGLE group. RFC 9309: a crawler obeys only its most specific
# matching group, so a named per-bot group would stop inheriting disallows added
# under *. Rules here apply to every crawler, AI and classic alike.
#
# No Sitemap line: no route is indexable, so the build emits no sitemap.xml.

User-agent: *
Allow: /
